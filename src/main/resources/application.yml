spring:
  application:
    name: securityproject
  datasource:
    url: jdbc:postgresql://localhost:9000/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8000
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs